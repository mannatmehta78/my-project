header { 
    border: 1px solid #0c0c0c ;
    background-color: green; 
    grid-column : 1 / span 2;
    display: flex;
    justify-content: space-between;
    align-items: center;
}
```
**header**  
- Adds a solid black border and green background to the header.
- Uses CSS Grid to span the header across two columns.
- Uses Flexbox to space out content horizontally and center items vertically.

````css
nav{
     border: 1px solid #ccc;
     background-color: grey;  
}
```
**nav**  
- Adds a light gray border and a grey background to the navigation bar.

````css
main { 
    border: 1px solid #ccc; 
}
```
**main**  
- Adds a light gray border to the main content area.

````css
footer { 
    border: 1px solid #0f0f0f;
    background-color: green; 
    grid-column : 1 / span 2;
}
```
**footer**  
- Adds a solid dark border and green background to the footer.
- Spans the footer across two columns in the grid layout.

````css
.dashboard {
    display: grid;
    grid-template-columns: 120px auto;
    grid-template-rows: 90px auto 50px;
    height: 100vh;
}
```
**.dashboard**  
- Sets up a CSS Grid layout for the dashboard.
- Defines two columns: a sidebar (120px) and a main area (auto).
- Defines three rows: header (90px), main content (auto), and footer (50px).
- Makes the dashboard fill the entire viewport height.

---

**Summary:**  
This CSS creates a structured dashboard layout using CSS Grid for overall placement and Flexbox for header alignment. Each section (header, nav, main, footer) is visually separated with borders and background colors. The dashboard is responsive and organized, making it suitable for an admin panel or similar application.**.dashboard**  
- Sets up a CSS Grid layout for the dashboard.
- Defines two columns: a sidebar (120px) and a main area (auto).
- Defines three rows: header (90px), main content (auto), and footer (50px).
- Makes the dashboard fill the entire viewport height.

---

**Summary:**  
This CSS creates a structured dashboard layout using CSS Grid for overall placement and Flexbox for header alignment. Each section (header, nav, main, footer) is visually separated with borders and background colors. The dashboard is responsive and organized,